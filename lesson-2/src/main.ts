let myName: string
let meaningOfLife: number;
let isLoading: boolean;
let album: string | number; // union type, either is ok

myName = '42'
meaningOfLife = 24
isLoading = false
album = '4'

const sum = (a:string, b:boolean) => {
    return a + b
}

let postId: string | number 
let isActive: number | boolean | string 

let re: RegExp = /\w+/g
